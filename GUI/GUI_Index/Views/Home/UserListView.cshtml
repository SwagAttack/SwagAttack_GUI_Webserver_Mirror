@using GUI_Index.Models
@model List<GUI_Index.Models.User>

    @{
        
    }

<body>
    <div class="container">
        <div class="row">
            <div class="absolute-center is-responsive">
                <table class="table table-hover">
                    <thead>
                    <tr>
                        <th scope="col"> @Html.DisplayName("Brugernavn")</th>
                        <th scope="col">@Html.DisplayName("Fornavn")</th>
                        <th scope="col">@Html.DisplayName("Efternavn")</th>
                        <th scope="col">@Html.DisplayName("E-mail")</th>
                    </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
    {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Username)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.GivenName)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.LastName)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Email)
                        </td>
                    </tr>
}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</body>
